<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="festusyuma.com.glaiddriver">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeIntroScreen">
        <activity android:name=".controller.ResetPasswordActivity"></activity>
        <activity android:name=".controller.ProblemsLoginActivity" />
        <activity android:name=".controller.RecoveryActivity" />
        <activity android:name=".controller.ForgotPasswordActivity" />
        <activity android:name=".controller.SignupOtpActivity" />
        <activity android:name=".controller.LoginActivity" />
        <activity android:name=".controller.SignUpActivity" />
        <activity android:name=".controller.GetStartedActivity" /> <!-- The below is for the splash screen and we need no action bar and the default theme -->
        <activity
            android:name=".controller.MainActivity"
            android:theme="@style/AppThemeIntroScreen">
            <intent-filter>

                <!-- MAIN represents that it is the Main Activity -->
                <action android:name="android.intent.action.MAIN" />
                <!-- Launcher Denotes that it will be the first launching activity -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.CarouselActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>